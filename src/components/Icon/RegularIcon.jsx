import styled from "styled-components";
import { IconContainer2 } from "../GlobalStyledComponents";

import React from 'react';

const RegularIcon = ({ src, alt }) => {
    return <IconContainer2 src={src} alt={alt} />
};

export default RegularIcon;
